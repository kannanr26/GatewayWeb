export default class Operator {
  constructor(operatorName, mobileNumber, password) {
    this.operatorName = operatorName;
    this.mobileNumber = this.mobileNumber;
    this.password = password;
  }
}
